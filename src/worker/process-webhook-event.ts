import { createAttachment, createNoteWithCategory } from "@/src/server/notes/repository";
import { parseCategory } from "@/src/server/notes/category-parser";
import { markEventCompleted, markEventFailed } from "@/src/server/webhook/event-repository";
import { query } from "@/src/server/db";
import { downloadLineContent } from "@/src/server/line/content-client";
import { saveFile } from "@/src/server/storage/file-store";
import { Readable } from "node:stream";
export async function processWebhookEvent(eventId: string) { const result = await query<Record<string, unknown>>(`SELECT * FROM webhook_events WHERE id = $1`, [eventId]); const event = result.rows[0]; if (!event) throw new Error("Webhook event not found"); try { const payload = event.payload_json as { message?: { type?: string; text?: string; id?: string; fileName?: string }; timestamp?: number; }; const message = payload.message; if (message) { const parsed = parseCategory(message.text ?? ""); const note = await createNoteWithCategory({ source: "line", contentType: message.type ?? "unknown", textContent: parsed.searchableText || null, categoryName: parsed.categoryName, lineMessageId: message.id ?? null, capturedAt: payload.timestamp ? new Date(payload.timestamp) : new Date() }); if (message.id && ["image", "video", "audio", "file"].includes(message.type ?? "")) { const response = await downloadLineContent(message.id); if (!response.body) throw new Error("LINE content response has no body"); const stored = await saveFile(Readable.fromWeb(response.body as never), message.fileName ?? `${message.id}.${message.type}`, response.headers.get("content-type") ?? "application/octet-stream"); await createAttachment({ noteId: note.id, lineMessageId: message.id, originalName: stored.originalName, mimeType: stored.mimeType, sizeBytes: stored.sizeBytes, storagePath: stored.storagePath, sha256: stored.sha256 }); } } await markEventCompleted(eventId); return { ok: true }; } catch (error) { await markEventFailed(eventId, error instanceof Error ? error.message : "Processing failed", true); return { ok: false }; } }
