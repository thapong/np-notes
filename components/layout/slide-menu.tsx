"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
const items = [["/", "Dashboard", "⌂"], ["/notes", "Notes", "▤"], ["/categories", "Categories", "#"], ["/webhook-events", "Webhook Events", "↔"], ["/settings", "Settings", "⚙"]];
export function SlideMenu({ open, onToggle }: { open: boolean; onToggle: () => void }) { const pathname = usePathname(); return <><aside className={`fixed inset-y-0 left-0 z-30 hidden border-r border-slate-200 bg-white shadow-sm transition-all md:block ${open ? "w-64" : "w-20"}`}><div className="flex h-20 items-center justify-between px-5"><span className={`font-bold tracking-tight text-indigo-600 ${open ? "" : "sr-only"}`}>NP Note</span><button aria-label="Toggle menu" onClick={onToggle} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100">{open ? "‹" : "›"}</button></div><nav className="space-y-1 px-3">{items.map(([href, label, icon]) => <Link key={href} href={href} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium ${pathname === href ? "bg-indigo-50 text-indigo-700" : "text-slate-600 hover:bg-slate-50"}`}><span>{icon}</span><span className={open ? "" : "sr-only"}>{label}</span></Link>)}</nav></aside><div className="flex items-center justify-between border-b border-slate-200 bg-white px-4 py-4 md:hidden"><span className="font-bold text-indigo-600">NP Note</span><button aria-label="Toggle menu" onClick={onToggle} className="rounded-lg border px-3 py-2">☰</button></div>{open && <div className="fixed inset-0 z-20 bg-slate-900/30 md:hidden" onClick={onToggle} />}{open && <aside className="fixed inset-y-0 left-0 z-40 w-72 bg-white p-5 shadow-xl md:hidden"><div className="mb-8 flex items-center justify-between"><b className="text-indigo-600">NP Note</b><button aria-label="Close menu" onClick={onToggle}>✕</button></div><nav className="space-y-2">{items.map(([href, label]) => <Link onClick={onToggle} key={href} href={href} className="block rounded-xl px-3 py-3 text-slate-700 hover:bg-indigo-50">{label}</Link>)}</nav></aside>}</> }
