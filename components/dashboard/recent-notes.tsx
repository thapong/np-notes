"use client";

import { useEffect, useState } from "react";

type Recent = { id: string; text_content: string | null; category_name: string | null };
export function RecentNotes() { const [notes, setNotes] = useState<Recent[]>([]); useEffect(() => { void fetch("/api/dashboard").then((response) => response.json()).then((data) => setNotes(data.recent)); }, []); return <section className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div className="flex items-center justify-between"><h2 className="font-semibold">Recent Notes</h2><a href="/notes" className="text-sm text-indigo-600">View all</a></div>{notes.length ? <div className="mt-5 divide-y divide-slate-100">{notes.map((note) => <article key={note.id} className="py-4"><div className="flex items-center justify-between gap-3"><p className="line-clamp-2 text-sm text-slate-700">{note.text_content || "ไม่มีข้อความ"}</p><span className="shrink-0 rounded-full bg-indigo-50 px-2 py-1 text-xs text-indigo-700">{note.category_name ?? "ยังไม่จัดหมวด"}</span></div></article>)}</div> : <div className="mt-8 rounded-xl border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500">ยังไม่มี Note — ส่งข้อความจาก LINE หรือสร้าง Note ใหม่</div>}</section>; }
