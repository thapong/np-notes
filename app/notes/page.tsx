"use client";
import { useState } from "react";
export default function NotesPage() { const [query, setQuery] = useState(""); return <div className="space-y-6"><header><p className="text-sm font-medium text-indigo-600">Workspace</p><h1 className="mt-1 text-3xl font-bold">Notes</h1><p className="mt-2 text-slate-500">ค้นหาและจัดการ Note ทั้งหมดของคุณ</p></header><div className="flex gap-3"><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="ค้นหาด้วยคำหรือความหมาย..." className="flex-1 rounded-xl border border-slate-200 bg-white px-4 py-3 outline-none ring-indigo-200 focus:ring-4" /><button className="rounded-xl bg-indigo-600 px-5 py-3 font-medium text-white">ค้นหา</button></div><div className="rounded-2xl bg-white p-8 text-center text-slate-500 shadow-sm ring-1 ring-slate-200">{query ? `กำลังค้นหา “${query}”` : "ยังไม่มีผลลัพธ์"}</div></div> }
